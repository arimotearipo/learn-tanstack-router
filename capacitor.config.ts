import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.learntanstackrouter',
  appName: 'learn-tanstack-router',
  webDir: 'dist'
};

export default config;
